# ==========================================================================
# ZONA PRIVADA — protege /zhara, /library y /ahorro con usuario y contraseña.
# ==========================================================================
AuthType Basic
AuthName "Zona privada"

# El fichero de contraseñas vive FUERA de www (no está en el repo ni es
# accesible por web ni lo pisa el deploy). Súbelo por FTP a /home/mpqsfjy/.htpasswd
AuthUserFile "/home/mpqsfjy/.htpasswd"

Require valid-user
