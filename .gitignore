# Dependencias (ya no se usan: todo va por CDN). Se ignoran por si acaso.
node_modules/

# Datos generados por las apps en el servidor
**/*-data.json

# Entorno / secretos
.env
.env.*
# Contraseñas Basic Auth: nunca al repo (van al servidor por FTP, fuera de www)
.htpasswd
**/.htpasswd

# Sistema
.DS_Store
Thumbs.db
*.log
